# Social Slash MCP Server - Slim dependencies for Docker
# Install: pip install -r requirements-mcp.txt

# Core
late-sdk==1.2.17
mcp>=1.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# HTTP
httpx>=0.27.0
requests>=2.31.0

# ASGI server (required for SSE transport)
uvicorn>=0.27.0

# AI providers (for agent tools)
google-genai>=1.0.0
anthropic>=0.40.0

# Image Processing (required by google-genai Imagen)
Pillow>=10.0.0

# Database (for engagement status)
aiosqlite>=0.19.0
